# Vorlage: nach .env kopieren und die echten Keys hinter das = einsetzen.
# Die echte .env ist per .gitignore ausgeschlossen und wird NIE committet.
# Verwendung: nur intern fuer den Uebersetzungslauf (scripts/translate_i18n.py).
DEEPL_API_KEY=
GOOGLE_TRANSLATE_API_KEY=
