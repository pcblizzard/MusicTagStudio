[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"

[project]
name = "musictagstudio"
version = "0.5.0"
description = "A safe, preview-first FLAC metadata editor"
requires-python = ">=3.12"
dependencies = [
    "PySide6>=6.8",
    "mutagen>=1.47",
]

[project.optional-dependencies]
dev = ["pytest>=9"]

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
